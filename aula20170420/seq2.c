#include <stdio.h>
int main()
{
float base, altura, area;
printf("Entre com a base do paralelogramo:");
scanf("%f", &base );
printf("Altura do paralelogramo: ");
scanf("%f", &altura );
area=(base*altura);
printf("A area do triangulo e: %g", area );
return 0;

}
