#include <stdio.h>
int main ( )
{
    float lado, area;
    printf("Entre com o lado do Quadrado: ");
    scanf("%f", &lado);
    area=lado*lado;
    printf("A area do quadrado e %g",area);



    return 0;
}
